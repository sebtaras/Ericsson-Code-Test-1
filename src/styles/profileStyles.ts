import {StyleSheet} from 'react-native';

export const profileStyles = StyleSheet.create({
  logOutButton: {
    position: 'absolute',
    bottom: 10,
    left: 10,
  },
  editButton: {
    borderWidth: 1,
    position: 'absolute',
    top: 10,
    right: 10,
  },
});
